using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace ProgamandoEmCSharp.average
{
    public class Guilds
    {
        // Guid ele e um global unique que serve para gerar um hash que indentificara algo de forma unica

        // Gerando um novo Guid
        var id = Guid.NewGuid();
        Console.WriteLine(id)

        //para tansoformar em uma string
        id.ToString();

        //assim vc consegue assimilar um guid
        var id2=new Guid("";)
        
    }
}